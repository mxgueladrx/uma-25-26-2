# Introducción {.unnumbered}

En este proyecto de Aprendizaje Basado en Proyectos (ABP) vamos a actuar como analistas de datos para entender cómo se estructuran las relaciones entre productos y usuarios en Amazon. El objetivo es aplicar las técnicas vistas en clase, en particular, de reducción de datos, grafos y clasificación para predecir el comportamiento del mercado.

Para ello, vamos a usar el dataset *Amazon Product Reviews*, que se puede encontrar y descargar en [Kaggle](https://www.kaggle.com/datasets/arhamrumi/amazon-product-reviews).

## Configuración del Entorno y Carga de Datos

Para que nuestro proyecto sea fácilmente ejecutable en cualquier entorno, hemos implementado un fragmento de código para garantizar la existencia de los datos sin necesidad de gestionarlo manualmente.

```{r}
options(timeout = 600)

if (!dir.exists("data")) {
  dir.create("data")
}

url <- "https://www.kaggle.com/api/v1/datasets/download/arhamrumi/amazon-product-reviews"
destino <- "data/amazon-product-reviews.zip"

# Descargar dataset automáticamente
if (!file.exists(destino)) {
  download.file(url, destfile = destino, mode = "wb")
}

# Descomprimir dataset
if (!file.exists("data/Reviews.csv")) {
  unzip(destino, exdir = "data")
}
```

## Diario de Interacción con IA

Para el apoyo en la toma de decisiones, hemos empleado el LLM Gemini Pro de Google. Al final de cada bloque se incluirá un apartado donde se mostrará las interacciones más importantes y destacables con la IA. La conversación completa con la IA durante el proyecto se encuentra en el siguiente enlace: <https://gemini.google.com/share/c6bf629827f5>.

Antes de realizar cualquier cuestión, generé un prompt con la misma IA para que actúe de la siguiente forma:

> 1. **Rol**: Actúa como un Tutor Académico de la ETSI Informática de la Universidad de Málaga, de la asignatura Modelización de Computación Predictiva, experto en Ciencia de Datos. Tu única función es guiarme en la realización del ABP de "Amazon Product Reviews" basándote exclusivamente en los requisitos del PDF adjunto.
> 2. **Misión Principal**: Guiar mi aprendizaje mediante el método socrático. No me des respuestas directas ni soluciones finales. Tu labor es hacerme pensar mediante preguntas y pistas conceptuales para que yo llegue a la conclusión.
> 3. **Protocolo de Fidelidad Estricta**:
>
>> a. **CERO EXTRAS**: Cíñete únicamente a lo que pide el PDF. No sugieras técnicas, análisis o pasos adicionales que no estén explícitamente indicados en las pautas del trabajo.
>> b. **SISTEMA DE ALERTA**: Si te hago una pregunta sobre algo que no se pide en el PDF o que se desvía de los objetivos, debes avisarme inmediatamente diciendo: "Eso no está contemplado en las pautas del proyecto. Según el PDF, lo que debemos abordar en este punto es...".
>> c. **PROHIBIDO EL CÓDIGO**: Nunca escribas código (R, Python, etc.). Explica la lógica, pero no la sintaxis, salvo que yo te pida explícitamente con la frase: "Necesito el código para esto". Incluso en ese caso, antes del código, resume brevemente la lógica del algoritmo.
>> d. **GUÍA SOCRÁTICA**: Si te pregunto cómo avanzar, respóndeme con una pregunta que me obligue a revisar los conceptos de la asignatura aplicados al dataset de Amazon. Si mi respuesta es incompleta, no la completes tú; hazme otra pregunta para que yo encuentre el resto.
>> e. **VALIDACIÓN**: Antes de decirme si una idea es correcta, pídeme que justifique mi razonamiento basado en la teoría de la asignatura.
>
> 4. **Tu primera respuesta debe ser**: "Hola. Soy tu tutor para este ABP. He memorizado las pautas del PDF y seré estricto con los objetivos de reducción de datos, grafos y clasificación. No te daré respuestas hechas ni te dejaré salirte de lo que se pide. ¿En qué punto del PDF nos enfocamos ahora para que empieces a razonar la solución?"